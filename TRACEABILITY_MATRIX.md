# MarlinSpike Bug-Fix Traceability Matrix

Tracks every finding from `MARLINSPIKE_BUG_REPORT.md` / `IMPACT_ANALYSIS.md` through to its
regression test and fix commit. Updated as each finding is worked, one at a time, TDD-first
(regression test written and confirmed RED against pre-fix code, then fix applied and confirmed
GREEN) going forward from Finding #3 onward.

Status values: `Fixed` (test + fix both in place, verified RED→GREEN) · `In Progress` ·
`Pending` (not yet started).

**Progress: 25 / 25 CRITICAL+HIGH fixed** — the entire CRITICAL+HIGH scope (#1–#25) is complete,
each TDD-first (regression test RED against pre-fix code → minimal fix → GREEN) with a full-suite
regression run recorded per finding. Clusters: web-security P0s (#1/#2/#3), recovery/run-lifecycle
(#9/#22/#23/#24/#25), enrichment integrity (#10), engine detection-correctness
(#12/#13/#14/#15/#16/#18), auth/tenancy/DoS (#4/#5/#6/#7/#8/#11), plugin+engine contracts
(#17/#20), live-capture (#21). Latest runs: main suite **443 passed / 7 skipped**; capd suite
**10 passed**. MEDIUM+LOW (#26–70) remain unscheduled.

## CRITICAL + HIGH (fix-order scope)

| # | Finding | Severity | Status | Regression Test | Fix Location | Notes |
|---|---|---|---|---|---|---|
| 1 | Stored XSS in Users page → admin takeover | CRITICAL | **Fixed** | `tests/test_users_xss.py` (3 structural tests; RED pattern confirmed present in pre-fix template — inline `onclick="USR.showLimits/deleteUser/showChpass"` handlers built by concatenating `esc(u.username)`, GREEN post-fix) | `marlinspike/templates/users.html` | Root cause: the row builder concatenated the username into inline `onclick` JS-string args; page `esc()` escapes `<>&` but NOT quotes, so `',alert(document.cookie),'` broke out and ran in an admin session. Fix rebuilds rows via `createElement`/`textContent`/`addEventListener` (handlers close over the raw username, never serialized to markup); removed now-unused `esc()`. Full suite 374 passed / 7 skipped. |
| 2 | Path traversal in preset sanitizer | CRITICAL | **Fixed** | `tests/test_preset_path_traversal.py` (2 e2e route tests, RED confirmed pre-fix — rename attempted `os.rename` on DATA_DIR, GREEN post-fix; 1 param unit test skips as sanitizer is a closure) | `marlinspike/app.py` (`_safe_preset_name`) | Root cause: `os.path.basename("..")` is `".."` and `.` is allowed by the charset regex, so `"."`/`".."` passed → `os.path.join(PRESETS_DIR, "..")` == DATA_DIR, letting delete-category `shutil.rmtree` the whole data dir. Fix rejects all-dot names. Full suite 371 passed / 7 skipped. |
| 3 | IDOR on `/api/runs/*` | CRITICAL | **Fixed** | `tests/test_run_ownership.py` (8 tests, RED confirmed pre-fix via `git stash`, GREEN confirmed post-fix) | `marlinspike/app.py` (`_run_owned_by_current_user` helper + 6 routes: list/status/output/stop/topology/live) | Full suite regression now clean (371 passed / 7 skipped) as of the #2 fix run |
| 4 | `deliver_reset_token` missing function | HIGH | **Fixed** | `tests/test_reset_token_delivery.py` (5 tests; RED confirmed pre-fix — function absent so import raised for file/log modes, GREEN post-fix; incl. username-sanitisation + 0600 perms + hook-override) | `marlinspike/auth.py` (`deliver_reset_token`, `set_reset_token_delivery`) | Implemented the missing delivery function: `file` mode writes `${DATA_DIR}/instance/reset-tokens/<safe-user>-<ts>.txt` (O_EXCL, 0600, path-traversal-safe username), `log` mode emits to server log, plus the override hook the docs reference. Self-service reset now works outside the default `disabled` mode. Full suite 419 passed / 7 skipped. |
| 5 | No rate limiting on expensive project endpoints | HIGH | **Fixed** | `tests/test_expensive_endpoint_rate_limit.py` (1 behavioral test; RED confirmed pre-fix — aggregate never 429'd over 40 requests, GREEN post-fix) | `marlinspike/app.py` (aggregate/ocsf/navigator/stix/sigma project routes, iocs-scan, per-report emit routes) | Added `@limiter.limit` to the previously unthrottled expensive endpoints (`default_limits=[]`): 20/min on project-wide aggregate/export/IOC-scan (each reads every report), 30/min on per-report emit/render. A noisy low-priv user can no longer starve the shared worker pool. Full suite 443 passed / 7 skipped. |
| 6 | `/api/reports/<filename>*` wrong directory + no ownership check | HIGH | **Fixed** | `tests/test_reports_shared_access.py` (3 tests; RED confirmed pre-fix — shared member 404'd on owner's report, GREEN post-fix; non-member-denied + owner-still-works) | `marlinspike/app.py` (`user_reports_dir`/`user_uploads_dir` via new `_owner_uid_for_project`) | Both helpers now resolve the path from the project OWNER's uid via access-checked `_get_project_for_user` (was `session['user_id']`), so shared members find the project's reports/uploads; an inaccessible `project_id` now `abort(404)`s instead of silently resolving to an empty own-uid dir. Full suite 422 passed / 7 skipped. |
| 7 | `/api/scans/start` ignores shared-member role model | HIGH | **Fixed** | `tests/test_scan_start_shared_role.py` (4 tests; RED confirmed pre-fix — shared editor 404'd, GREEN post-fix; viewer-denied + owner + non-member cases) | `marlinspike/app.py` (`api_scan_start`) | Replaced owner-only `Project.query.filter_by(id, user_id=self)` with access-checked `_get_project_for_user(pid, min_role="editor")` — editors/owners may scan, viewers (read-only) cannot. Combined with #6's owner-based dirs, an editor's scan now lands in the correct (owner's) project dir. Full suite 426 passed / 7 skipped. |
| 8 | TOCTOU race on scan concurrency limits | HIGH | **Fixed** | `tests/test_scan_concurrency_toctou.py` (3 tests; RED confirmed pre-fix — reserve/release helpers absent, GREEN post-fix; reservation-counts-immediately, release-frees-slot, release-won't-evict-running) | `marlinspike/app.py` (`_reserve_scan_slot`/`_release_scan_slot`, `api_scan_start`) | Check-and-reserve now happen in one `_runs_lock` critical section via a `pending` placeholder (counted by `_get_active_runs`), so a reservation is visible to the next check — closing the window between the limit check and run registration. Reservation released on spawn failure. (Memory backend + single-worker; cross-worker db-mode still needs DB-level locking — noted as follow-up.) Full suite 429 passed / 7 skipped. |
| 9 | Recovery path bypasses enrichment plugins | HIGH | **Fixed** | `tests/test_recovery_enrichment.py` (2 tests; RED confirmed pre-fix — recovery ingested a completed report without calling enrichment, GREEN post-fix; enrichment-failure→degraded case) | `marlinspike/recovery.py` (`_ingest_completed_report`) | Recovery-completed reports now run standalone `enrich.run_all` (no Flask/DB), producing the same sidecars a normal finalize would; total enrichment failure sets `recovery_state=reaped_completed_degraded` + durable `error_tail` (reuses #10's degraded concept). Covers both reap-completed and re-attach-watcher-completed paths. Full suite 382 passed / 7 skipped. |
| 10 | Plugin failures swallowed, run still "completed" | HIGH | **Fixed** | `tests/test_finalize_enrichment_degraded.py` (2 tests; RED confirmed pre-fix — a raising plugin left no degraded signal, GREEN post-fix; clean-run negative case) | `marlinspike/app.py` (`_finalize_run`, `_finalize_scan_history`, `/api/runs/<id>/status` + list) | Introduces the shared **degraded** concept (reused by #9/#25): enrichment-plugin exceptions/missing-report are tracked; run stays `completed` (primary report valid, all UI keys on it) but carries `enrichment_degraded` + failed-plugin list + `degraded_reason`, persisted durably to `ScanHistory.error_tail` and surfaced in status/list endpoints. Full suite 380 passed / 7 skipped. |
| 11 | No `MAX_CONTENT_LENGTH`; size check after full body spooled | HIGH | **Fixed** | `tests/test_max_content_length.py` (2 tests; RED confirmed pre-fix — `MAX_CONTENT_LENGTH` unset/None, GREEN post-fix; oversized body → 413) | `marlinspike/config.py` (`MAX_CONTENT_LENGTH`) + `marlinspike/app.py` (`create_app`) | Added a framework-level body cap (`PCAP_MAX_SIZE` + 16 MB slack, env-overridable) so Werkzeug rejects oversized/lying bodies with 413 before any view spools them, on every endpoint; the per-view streaming check remains as second-line defence. Full suite 431 passed / 7 skipped. |
| 12 | Benign NTP/DNS traffic flagged as C2 beaconing | HIGH | **Fixed** | `tests/test_beacon_benign_ntp_dns.py` (4 tests; RED confirmed pre-fix — NTP/DNS to public flagged CRITICAL, GREEN post-fix; 443/high-port real-C2 guardrails still fire) | `marlinspike/engine.py` (`BENIGN_PERIODIC_PORTS`, `_check_c2_indicators`) | Added a deliberately-narrow benign-periodic port set (DNS 53 / mDNS 5353 / NTP 123) and skip beaconing classification for them; 443 and arbitrary high ports (real C2 channels) unaffected, and DNS tunnelling still caught by the entropy detector. Full suite 406 passed / 7 skipped. |
| 13 | DNS entropy thresholds unreachable for short labels | HIGH | **Fixed** | `tests/test_dns_exfil_short_labels.py` (3 tests; RED confirmed pre-fix — 60-fanout 8-char exfil undetected, ratio fn absent, GREEN post-fix; benign-low-ratio negative case) | `marlinspike/engine.py` (`_compute_dns_entropy_from_queries`, `RiskSurface._check_c2_indicators`) | Root cause: absolute Shannon entropy is length-capped at `log2(L)`, so `>4.0`/`>3.5` gates need labels ≥17/≥12 chars — a random 8-char label maxes at 3.0 and can never trip. Fix adds a length-normalized ratio `H/log2(L)` (Conversation.`dns_entropy_ratio`) and lets CRITICAL (>50-fanout) / HIGH (high-volume) gates fire on ratio>0.85/0.80; MEDIUM left absolute-only to avoid low-volume false positives. Full suite 394 passed / 7 skipped. |
| 14 | `_ip_in_subnet` naive string-prefix match | HIGH | **Fixed** | `tests/test_ip_in_subnet.py` (5 tests; RED confirmed pre-fix — /16, /25, and /32-host cases wrong, GREEN post-fix) | `marlinspike/engine.py` (`_ip_in_subnet`) | Replaced `ip.startswith(first-3-octets)` (ignored CIDR length; /16 matched only the third octet, /25 wrong, string-prefix false pos/neg) with stdlib `ipaddress` CIDR membership; invalid input returns False. Fixes silent Purdue-zone misclassification for `--subnet-map` overrides. Full suite 402 passed / 7 skipped. |
| 15 | VMware OUI `00:50:56` mislabeled as Rockwell Automation | HIGH | **Fixed** | `tests/test_vmware_oui.py` (3 tests; RED confirmed pre-fix — merged db resolved 00:50:56 to Rockwell, GREEN post-fix; IEEE-source VMware check) | `marlinspike/engine.py` (`ICS_OUI_DB`) | Removed the bogus `00:50:56 → Rockwell Automation` overlay (it shadowed the correct IEEE `VMware, Inc.` entry). VMware VMs no longer fingerprinted as OT hardware / given inflated attack priority. Aside: `_load_oui_db` looks for `oui.json` under `marlinspike/` but the file ships at repo-root `data/` — the IEEE db isn't loaded in this layout (potential separate finding). Full suite 397 passed / 7 skipped. |
| 16 | Malware IOC stage never runs on chunked pipeline | HIGH | **Fixed** | `tests/test_chunked_malware_stage.py` (1 test; RED confirmed pre-fix — `run_chain_from_conversations` never invoked `_run_malware_stage`, GREEN post-fix) | `marlinspike/engine.py` (`run_chain_from_conversations`) | Added the Stage 4b malware/IOC block mirroring `run_chain`, deriving events from the merged conversations. Every large-PCAP scan (the documented chunked path via `chain-from-conversations`) now runs IOC matching instead of silently omitting it. Full suite 386 passed / 7 skipped. |
| 17 | `--chunk-size` ignored when Rust DPI engine selected | HIGH | **Fixed** | `tests/test_chunk_size_rust_dpi.py` (3 tests; RED confirmed pre-fix — `_run_marlinspike_dpi` had no chunk param and the Rust path dropped `--chunk-size`, GREEN post-fix; forwards when set, omits when 0) | `marlinspike/engine.py` (`_run_marlinspike_dpi`, `_dissect_with_selected_engine`) | The Rust path now forwards `--chunk-size` to the binary (was passed only to the Python fallback). In `auto` mode a binary predating the flag falls back to the Python chunked dissector, so memory stays bounded either way. (Rust binary not present in this checkout — Docker-pinned; forwarding + fallback verified via mocks, not against the live binary.) Full suite 442 passed / 7 skipped. |
| 18 | Wildcard domain IOCs never match anything | HIGH | **Fixed** | `tests/test_iocs_wildcard_domain.py` (5 tests; RED confirmed pre-fix — `*.evil.com` matched neither subdomain nor apex, GREEN post-fix; lookalike/false-positive + plain-domain cases) | `marlinspike/iocs.py` (`_scan_report` domain branch) | Root cause: value normalised to literal `"*.evil.com"` and matched via `norm in query` substring, which a real query never contains. Fix strips leading `*.` to a base domain and matches exact-or-subdomain (`c == base or c.endswith("."+base)`), also removing loose-substring lookalike false positives. Full suite 391 passed / 7 skipped. |
| 19 | Audit log write failures silently swallowed | HIGH | **Fixed** | `tests/test_audit_failure_visibility.py` (3 tests; RED confirmed pre-fix — no counter, failure logged only as WARNING naming event_type, GREEN post-fix; still-never-raises case) | `marlinspike/audit.py` (`audit`, `get_audit_failure_count`) | On write failure the full event (type/actor/target/status/ip/detail) is now emitted as a structured ERROR-level fallback record (recoverable compliance line) and counted via `get_audit_failure_count()`, instead of a bare WARNING that dropped the event content. Still never raises. Full suite 434 passed / 7 skipped. |
| 20 | MITRE plugin missing `severity` rule filter | HIGH | **Fixed** | `tests/test_mitre_severity_filter.py` (5 tests; RED confirmed pre-fix — `when.severity` ignored so rules over-matched, GREEN post-fix; case-insensitive + back-compat + category+severity cases) | `plugins/marlinspike_mitre/plugin.py` (`_build_context`, `_primitive_matches`) | Implemented the documented `when.severity` gate: added a `severity_counts` index over risk findings and a membership-check primitive (consistent with `finding_categories`/`indicator_types`). Rules that gate on HIGH/CRITICAL no longer fire on everything. Full suite 439 passed / 7 skipped. |
| 21 | capd has no interface-level session lock | HIGH | **Fixed** | `marlinspike-capd/tests/test_interface_lock.py` (4 tests; RED confirmed pre-fix — 2nd session on same NIC allowed, GREEN post-fix; distinct-interfaces + `any`-blocks-named + named-blocks-`any`) | `marlinspike-capd/capd/server.py` (`_start_session`) | Added the interface-level lock inside `_sessions_lock` (atomic vs concurrent starts): a named NIC hosts one running capture at a time, and `any` conflicts with / is blocked by any other running capture — now matching the contract already documented in `docs/live-capture.md`. capd suite 10 passed; main suite 443 passed / 7 skipped. |
| 22 | Reaper timeout check bypasses PID-liveness check | HIGH | **Fixed** | `tests/test_recovery_timeout_liveness.py` (3 tests; RED confirmed pre-fix — past-deadline runs failed even when engine alive or report complete, GREEN post-fix; genuine-abandonment guardrail) | `marlinspike/recovery.py` (`reap_orphan_runs`) | Reordered so the deadline verdict is last resort: a past-deadline run is only abandoned if the engine is dead AND no complete report exists. A live engine is re-attached (slow-but-healthy), a complete report is ingested as completed — good reports are no longer discarded / shown as false failures. Full suite 409 passed / 7 skipped. |
| 23 | PID-reuse defense uses substring match not token match | HIGH | **Fixed** | `tests/test_recovery_pid_reuse.py` (5 tests; RED confirmed pre-fix — `/opt/marlinspike-tools/daemon` substring-matched, GREEN post-fix) + updated 2 existing recovery tests that embedded the token in a `-c` code string (they relied on the old substring behavior) to pass it as a real argv element | `marlinspike/recovery.py` (`_live_argv_matches`) | Changed `token in " ".join(actual)` (substring) to exact whole-element membership (`token in set(actual)`); an unrelated process whose path merely contains `marlinspike` is no longer mistaken for the live engine. Full suite 414 passed / 7 skipped. |
| 24 | `reap_orphan_runs` ignores RUN_STORE mode, multi-worker race | HIGH | **Fixed** | `tests/test_recovery_multiworker.py` (4 tests; RED confirmed pre-fix — `claim_for_recovery` absent + reaper finalized an already-claimed row, GREEN post-fix) | `marlinspike/run_store.py` (`claim_for_recovery`) + `marlinspike/recovery.py` (`reap_orphan_runs` claim gate) | Root cause: under gunicorn `-w N`, `create_app()` runs the reaper once per worker concurrently; each independently reconciled every `running` row → N watcher threads per live orphan, N `record_finish` per dead one (duplicate finalization). Fix: atomic single-owner claim via conditional UPDATE (`status='running' AND recovery_state IS NULL` → `'claimed'`) through the durable run store; only the winning worker reconciles. Full suite 378 passed / 7 skipped. mypy clean on new code (pre-existing warn-only errors unchanged). |
| 25 | Reaper races app finalize pipeline, drops enrichment silently | HIGH | **Fixed** | `tests/test_recovery_finalize_race.py` (3 tests; RED confirmed pre-fix — `mark_finalizing` absent + stale finalizing run failed-by-deadline instead of completing, GREEN post-fix; fresh-finalizing-skip already held via #24) | `marlinspike/run_store.py` (`mark_finalizing`/`reclaim_finalizing`) + `marlinspike/recovery.py` (reaper) + `marlinspike/app.py` (`_finalize_run`/`_finalize_scan_history`) | Live finalize claims `recovery_state='finalizing'` before enrichment; reaper defers to a fresh finalizing run but reclaims a stale one (past deadline → worker crashed) and reconciles it as completed, re-running enrichment via #9. Live completion clears the transient marker. Full suite 385 passed / 7 skipped. |

## MEDIUM + LOW (#26–70)

The detailed `MARLINSPIKE_BUG_REPORT.md` is not present in this tree; these are being worked from
the grouped impact notes in `IMPACT_ANALYSIS.md`, each defect verified against the actual code
before fixing. Same TDD discipline and columns as above. Finding numbers within a theme cluster are
assigned in the order worked (the exact original numbering isn't recoverable without the report).

### Emit-format correctness (#43–46)

| # | Finding | Severity | Status | Regression Test | Fix Location | Notes |
|---|---|---|---|---|---|---|
| 43 | OCSF Detection Finding missing required `time` | MEDIUM | **Fixed** | `tests/test_emit_format_fixes.py::test_ocsf_record_has_time_even_without_report_timestamp` (RED pre-fix — `time` None→pruned when report had no timestamp) | `marlinspike/emit/ocsf.py` (`_report_time_ms`) | `time` is a required OCSF attr; now falls back to emit time instead of None (which `_prune` dropped → invalid record). Full suite 449 passed / 7 skipped. |
| 44 | Sigma invalid empty `\|in: []` selectors | MEDIUM | **Fixed** | `tests/test_emit_format_fixes.py::test_sigma_no_empty_selectors_when_no_nodes` (RED pre-fix) | `marlinspike/emit/sigma.py` (`_rule_cross_purdue`, `_rule_modbus_write_anon`, `render_rules`) | Cross-Purdue with no nodes is skipped; modbus allowlist filter omitted when empty — no more empty selectors that match nothing / get rejected. |
| 45 | Sigma ATT&CK tag wrong format (`attack.t1071_001`) | MEDIUM | **Fixed** | `tests/test_emit_format_fixes.py::test_sigma_attack_tag_uses_dotted_subtechnique` + updated `test_sigma_emit.py::test_attack_tags` (encoded the bug) | `marlinspike/emit/sigma.py` (`_attack_tags`) | Keep SigmaHQ's dotted sub-technique convention `attack.t1071.001` (was underscore, an unrecognised tag). |
| 46 | STIX IPv6 mis-typed as `mac-addr`/`ipv4-addr` | MEDIUM | **Fixed** | `tests/test_emit_format_fixes.py` (3 STIX tests; RED pre-fix on short + long IPv6) | `marlinspike/emit/stix.py` (`_indicator_pattern_for_finding`) | Classify via `ipaddress` (+ MAC regex) instead of `":" in node and len<=17`: IPv6 → `ipv6-addr`, MAC → `mac-addr`, IPv4 → `ipv4-addr`. |

### Project-sharing correctness (#29/#32/#34/#35 cluster)

| # | Finding | Severity | Status | Regression Test | Fix Location | Notes |
|---|---|---|---|---|---|---|
| 34 | Aggregate dedup splits one device into two assets | MEDIUM | **Fixed** | `tests/test_aggregate_asset_dedup.py` (3 tests; RED pre-fix — MAC-in-one-report + IP-only-in-another became 2 assets, GREEN post-fix; order-independent + distinct-devices-stay-separate) | `marlinspike/aggregate.py` (`_resolve_asset_key`, pass-1 IP→MAC map) | Project Overview keys assets by MAC-else-IP; an IP-only sighting (report where the MAC wasn't captured) now resolves to the device's known MAC key via a project-wide IP→MAC map, so it stays one asset. Full suite 452 passed / 7 skipped. |
| 32 | Generic `/api/upload` project resolution owner-only | MEDIUM | **Fixed** | `tests/test_upload_shared_role.py` (2 tests; RED pre-fix — shared editor 404'd, GREEN post-fix; non-member denied) | `marlinspike/app.py` (`_handle_upload`) | The generic upload path resolved the target project with an owner-only `filter_by(id, user_id=self)` (the project-scoped `/api/projects/<pid>/upload` was already correct). Now uses `_get_project_for_user(pid, min_role="editor")` — a shared editor's upload reaches the project (and, via #6, lands in the owner's dir). Full suite 454 passed / 7 skipped. |
| 35 | Orphaned files on disk when a non-creator deletes a project | MEDIUM | **Fixed** | `tests/test_project_delete_cleanup.py` (RED pre-fix — owner-role member's delete left the creator's files on disk, GREEN post-fix) | `marlinspike/app.py` (`api_projects_delete`) | Delete built upload/report paths from `session['user_id']` (the deleter), but files live under `proj.user_id`; a non-creator `owner`-role member deleting rmtree'd the wrong (empty) dir → real files orphaned forever. Now uses the owner uid and dissociates all project scans (not just the deleter's). Full suite 455 passed / 7 skipped. |

### Recovery / concurrency plumbing (#54–56, #68 cluster)

| # | Finding | Severity | Status | Regression Test | Fix Location | Notes |
|---|---|---|---|---|---|---|
| 54 | Stale `engine_pid` on terminal runs | LOW | **Fixed** | `tests/test_finalize_clears_pid.py` (2 tests; RED pre-fix — completed/failed rows kept the PID, GREEN post-fix) | `marlinspike/app.py` (`_finalize_scan_history`) | The recovery path (`record_finish`) cleared `engine_pid` on terminal, but the normal live finalize never did — leaving a stale PID on every completed row (a landmine for PID-based tooling / PID reuse). Now cleared on terminal, mirroring `record_finish`. Full suite 464 passed / 7 skipped. |
| 55 | Silent in-memory-DB fallback | MEDIUM | **Fixed** | `tests/test_inmemory_db_warning.py` (2 tests; RED pre-fix — only a `log.debug`, GREEN post-fix; no warning when a real DB URL is set) | `marlinspike/app.py` (`create_app`) | Falling back to `sqlite:///:memory:` (only reachable via the test escape hatch `MARLINSPIKE_ALLOW_NO_DATABASE_URL=true`) now logs a prominent WARNING that data is ephemeral and not shared across workers — so a lingering env var + a real server start is no longer silent. Full suite 459 passed / 7 skipped. |
| 68 | Missing index on `scan_history.status` | LOW | **Fixed** | `tests/test_scan_history_index.py` (2 tests; RED pre-fix — no index, GREEN post-fix) + `test_alembic_migrations.py` (chain upgrades) | `marlinspike/models.py` (`ScanHistory.__table_args__`) + `migrations/versions/0003_scan_history_status_index.py` | Composite `(status, user_id)` index: leading `status` serves the reaper's per-boot `status="running"` scan, full index serves the db-mode per-user concurrency count. Model change covers fresh installs; migration 0003 covers existing DBs. Full suite 457 passed / 7 skipped. |

### Additional bugs found post-review / from user reports (not in the original 70)

| Finding | Severity | Status | Regression Test | Fix Location | Notes |
|---|---|---|---|---|---|
| IEEE OUI database never loaded (vendor fingerprinting degraded) | MEDIUM | **Fixed** | `tests/test_oui_db_load.py` (3 tests; RED pre-fix — only ~20 ICS entries, GREEN post-fix — >1000 entries + VMware resolves) | `marlinspike/engine.py` (`TopologyBuilder._load_oui_db`) | Searched only the package dir; the file ships at repo-root `data/oui.json` (dev) and `/app/oui.json` (Docker). Now searches PROJECT_ROOT / DATA_DIR / package dir + `MARLINSPIKE_OUI_DB` env override. Full suite 462 passed / 7 skipped. |
| Docker build fails — `protoc` missing in Rust builder stages | HIGH (build blocker) | **Fixed** | Manual (build-time; verified against user report) | `Dockerfile` (dpi-builder, malware-builder) | Added `protobuf-compiler` + `libprotobuf-dev`; users no longer patch the Dockerfile to build. |
| Live-capture interface dropdown dumps raw `503 {…}` blob | LOW (cosmetic) | **Fixed** | Live render check (HTTP 200) | `marlinspike/templates/capture.html` | Shows a clean "disabled" label when live capture is off, instead of the raw API error. |

### Remaining MEDIUM + LOW (#26–31, #33, #36–42, #47–67, #69–70)

Not yet worked. Grouped by theme in `IMPACT_ANALYSIS.md`: remaining recovery/concurrency plumbing
(stale `engine_pid`, silent in-memory-DB fallback), extension-contract gaps, capture-sidecar races,
and assorted LOW items.

## Process notes

- Each row moves `Pending → In Progress → Fixed` one at a time — no batching.
- "Fixed" requires: a regression test that reproduces the bug, confirmed to fail against pre-fix
  code (RED), the minimal fix applied, and the same test confirmed passing (GREEN).
- A full existing-suite regression run (`pytest tests/ -q`) is recorded per finding once
  performed; until then, treat "Fixed" as verified only for that finding's own new test(s), not
  yet cleared of side effects on the rest of the suite.
