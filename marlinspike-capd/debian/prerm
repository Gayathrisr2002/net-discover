#!/bin/sh
# prerm for marlinspike-capd
set -e

case "$1" in
  remove|deconfigure)
    if [ -d /run/systemd/system ]; then
      systemctl stop marlinspike-capd 2>/dev/null || true
      systemctl disable marlinspike-capd 2>/dev/null || true
    fi
    ;;
esac

exit 0
